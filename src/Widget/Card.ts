export class ShopLayout{
    constructor(
        private _parent:HTMLElement
    ){
        this._parent.classList.add("container")
    }
}

export class Card{
    constructor(
        private _parent:HTMLElement
    ){
        this._parent.classList.add()
    }
}